# Core ML stack
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.11.1
trl>=0.9.4
# 4-bit quantization (not supported on Windows officially)
bitsandbytes>=0.43.3; platform_system != "Windows"
# Data
datasets>=2.20.0
yfinance>=0.2.41
pandas>=2.2.2
numpy>=1.26.4
scikit-learn>=1.3.0
# Torch (CPU build if no CUDA)
torch>=2.3.1
# Sometimes required for tokenizers
sentencepiece>=0.1.99
protobuf>=4.25.3
